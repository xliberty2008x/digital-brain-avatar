# Branch protection validation

This documentation-only change verifies that pull requests to `master` must
complete the repository CI, GitGuardian, and central Grok App review before
merge. It does not change or execute project code.
